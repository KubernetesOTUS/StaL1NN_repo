apiVersion: v1
kind: ConfigMap
metadata:
  name: configuration
  namespace: homework
data:
  nginx.conf: |-
      server {
          listen 0.0.0.0:8000;
          server_name  localhost;
          location / {
            root   /homework;
            index.html;
        }
      }
